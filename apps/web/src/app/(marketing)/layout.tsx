// Marketing layout — shared header/footer for public pages
// TODO: [Sprint 4.1] — Wren adds full nav/footer
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
